- [2026-07-23T19:05:00-0400] CONSOLIDATED AT MANAGER SHIFT BOUNDARY by cockpit, operator-approved (buckets: retire 8 discharged lines / compress 6 doctrine lines into the disciplines + parent lines / keep live regimes; history: this file entered git 2026-07-23 evening (SABLE-ztks1) — before that, bd shift/day records are the only history and the earlier "git-log of this file" claim here was false; day narrative in SABLE-1mk8f, prior consolidation 2026-07-21). One pre-approved verify-then-retire item pending: the description-borne rule (chuck's five-rung + both-doors on the landed rendering fix, post-activation). The stranded-floor item was DISCHARGED 2026-07-23 evening by cockpit verification (see its dated retirement line below).
- [2026-07-21T13:42:32-0400] Merges go ONLY through sable-merge-gate; NO bare git merge/push on any integration branch. Workers self-push their worktree branch; post-push hook files for-chuck; Chuck merges via the gate. [2026-07-23 coverage note, measured: the landed rrn6r guard enforces this on agent-Bash pushes ONLY — gate promotions push via python subprocess and terminal pushes are covered by NOTHING until SABLE-eoytq's ruleset; the contract binds conduct, the guard covers one path.]
- [2026-07-21T13:42:52-0400] STATE MODEL: MERGED != INSTALLED != WIRED != RESOLVED != DELIVERED (five rungs, SABLE-xbwo2 owns the standing wiring check; SABLE-4xj4y installer symlink defect open), and ACTIVATION IS PER-FILE, never per-bead. ACTIVATION SHAPES BY FILE CLASS: plain-pinned copy -> PIN REFRESH; snapshot-pinned tool -> --pin-snapshot re-pin; SYMLINKED bin -> LOCAL PULL of the shared checkout (symlinked bins run STALE until the pull; merged-but-dark applies); repo-side files -> merge or own install step. Classify FIRST (sable-bin-install --classify PLUS test -L on the INSTALLED path, per-file — skill dirs are MIXED per file), then apply the matching shape. Acceptance observations count only after activation is verified (qp92x: ask what would have to be true for the observation to come out differently — if nothing, it is zero evidence).
- [2026-07-21T15:50:46-0400] BINS POLICY (pin facts current as of 2026-07-23 evening): sable-merge-gate is a VERSIONED-SNAPSHOT pin, currently sable-d75552e5bba0 (re-pinned three times 2026-07-23 for gate-lib landings, all declared events with both-sides activation proof; third re-pin cockpit-verified independently: installed readlink -> sable-d75552e5bba0, snapshot promote-lib md5 5be3030e = HEAD, other two pins untouched); sable-reconcile-handoffs pinned copy sable-c75137b2a6f5; sable-dolt-push pinned copy; sable-spawn-worker + sable-msg stay LIVE SYMLINKS (python sibling-lib imports — naive copies sever them; spawn-worker content currently carries the fz8kd+activation, cd051ae-era). IMPORT-CHECK BEFORE CONVERT gates any future pin. Refreshes are DECLARED events with before/after verification on the pinned set; '--repin' reverts OTHER pins to symlinks (33hw3) — re-verify all pins after any repin. Pinning-adjacent test suites MUST scope SABLE_LIB_DIR to scratch (33hw3 contamination incident).
- [2026-07-21T13:43:17-0400] RETIRE-ON-ACTIVATION (fleet-wide, binding, BOTH legs): a prose convention retires only when its replacing mechanism is (leg 1) VERIFIED FIRING from a live pane — never on merge or close — AND (leg 2) its DEGRADED path is demonstrated LOUD through BOTH doors (dependency ABSENT and PRESENT-BUT-UNAVAILABLE). [2026-07-23 amendment, chuck's sm269 measurement: WHICH DOOR IS SILENT IS A PER-MECHANISM PROPERTY — MEASURE BOTH EVERY TIME; wezu1's absence-loud geometry is a common case, not a rule (notes-clobber-guard is exactly inverted: bd-ABSENT is the silent door). A fail-open DECISION may stand; the REPORT must be loud; if any door degrades to silence the convention does NOT retire.] Live consequence: the --append-notes-never---notes discipline stays despite its guard being MERGED+INSTALLED+WIRED+DELIVERED-discriminating, until SABLE-4cad2 makes Door A loud or states the same-PATH assumption.
- [2026-07-23T20:20:00-0400] STRANDED-FLOOR LINE RETIRED (cockpit verification, operator pre-approved): positive sample = wk-provenance-guard end-to-end — genuine missed-handoff branch, resolved to SABLE-rrn6r via the structured branch= metadata leg (correct bead, correct filing-time status on SABLE-pmpd2), seat hold-with-reason as durable metadata the floor respected (no re-file), revise 36d5aa9 verified contained in origin/tmux-only, work bead closed terminal. Hand-enumeration at wind-down NO LONGER REQUIRED. Full evidence in SABLE-i5739 notes.
- [2026-07-21T13:43:50-0400] STANDING DISCIPLINES: (1) NOTES — use --append-notes, never --notes; guard landed but Door A silent, discipline holds until SABLE-4cad2 (see retire-on-activation line). Never pass inline bodies containing backticks/$()/apostrophes to bd remember / bd q / --append-notes — compose via python argv wrapper or --body-file where supported (four silent corruptions 07-22, a fifth 07-23 caught by its author; SABLE-qwthx signed-not-landed, SABLE-eu4g3 open). (2) POOL HYGIENE: non-dispatchable new beads get bd defer with an explicit un-defer condition; close-beats-defer for served coordination beads; bd ready means dispatchable-NOW; never defer a P1 blocker for hygiene. (3) INTERRUPT SAFETY: on a DIALOG-STALLED read check the working indicator first; --interrupt only with dialog markers present AND working indicator absent. (4) NEGATIVE EVIDENCE: any absence-asserting probe validates against a known positive first; verify a cited line's REACHABILITY before shipping a correction; never report a count you cannot explain per-pid. (5) SINGLE WIRING: exactly one wired copy per hook family (global ~/.claude). (6) IRON RULE regression suites are non-negotiable on any merge-spine branch. (7) SILENT-INSTRUMENT CONTRACT: 'could not assess' is a DISTINCT, LOUD outcome, never empty output; decision axis (fail-open/closed) ruled per-site, report axis always loud, the two never traded. (8) CLOSE-BEFORE-GREEN REQUIRES A WATCHED HOLD: stated lift condition + named owner + named re-opener, first-class metadata never prose (zvx4i; o9b8u/50z5g precedents; optimus runs a pre-close hold-sweep as standing practice). (9) STRANDED-CLAIM RELEASE IS THREE-STATE: in_progress+RUNNING pane = leave; +DONE pane = never release (finished awaiting close); +NO pane = release and log; fourth shape no-pane+work-MERGED = stranded-complete, seat presence-probe close. Reap panes only AFTER bead terminal state, with a pre-destruction evidence read (three pane-only sweep results saved 07-23; SABLE-hjkqf mechanizes). INSTRUMENT CAUTION until SABLE-h1fa7: sable-worker-status silently lane-scopes — fleet questions go to direct tmux enumeration. (10) BROKERED STAND-DOWN IS FIVE PARTS: named lanes, stated duration, named owner, ps confirmed both ends, EXPLICIT statement of suspended activity; a read that perturbs a load-sensitive measurement is a write. (11) SHA REFERENCES — pointer vs description (canonical on SABLE-9u86l): a POINTER to something re-derivable (the moving spine) -> quote nothing, resolve at use; a DESCRIPTION of something that must be preserved (work under revise) -> state it plus the auto-rebase expectation. (12) PURPOSE IS NOT COVERAGE: a guard's coverage is established only by reading its wiring/matcher, never inferred from its purpose. (13) DERIVATIONS HAVE A SHELF LIFE: re-derive verdict refs/pin coherence in the same breath as the action that consumes them; queue reports carry a BINDS-CURRENT-PAIR column. (14) bd list TRUNCATES SILENTLY AT 50: any absence conclusion requires --limit 0 (until wk-bdlist-truncation lands; SABLE-52aym). (15) sable-contained is the mandated tool for any containment call; a COULD-NOT-ASSESS from it is HOLD-and-hand-check, never noise.
- [2026-07-22T23:55:00-0400] INSTALL-SHAPE CLAIMS ARE CLAIMS, NOT BACKGROUND: before any activation claim, run test -L on the INSTALLED path of every file in the landing footprint and state the shape PER FILE, emitting the DISTINCTION. A worked example inside doctrine is the most dangerous inherited claim — it arrives pre-authorised (the worker-dispatch.md symlink survived four agents in one night). Banking a lesson does not install it.
- [2026-07-23T12:40:00-0400] (a) WORKER CAP 12 (operator; SABLE_MAX_WORKERS=12 per-invocation; count RUNNING WORK not panes). (b) DESCRIPTION-BORNE BRIEFS: content a WORKER must see goes in the DESCRIPTION (--append-notes for gate/audit content only) — the h8swc fix (notes now render) is LANDED-NOT-ACTIVATED on wk-dispatch-delivery; this rule retires on chuck's five-rung + both-doors evidence AFTER that branch activates (operator pre-approved; gate stated in SABLE-bcni2), not on its merge. Companion landed same branch: kv44f exit-13 guard (empty description + non-trivial notes refuses loudly).
- [2026-07-23T13:45:00-0400] A BRIEF CANNOT BIND A PRE-SPAWN ACTOR (binding until SABLE-26ipr + SABLE-9u86l land): refresh_worktree rebases a REUSED worktree onto origin/tmux-only BEFORE the worker reads its prompt, silent on success. (a) NO dispatch of any kind into a frozen/held tree — the spawn itself rewrites the protected history; (b) revise-brief tip SHAs are stale by construction when the spine moves — state SHA plus auto-rebase expectation (see discipline 11); (c) description-borne rules govern what the worker READS, not what the tool does first. Fix owners: 26ipr (loud fail-closed refusal, both spawn and push legs), 9u86l (announce old->new SHA into the prompt).
- [2026-07-23T14:00:00-0400] SABLE_MG_OPTIMISTIC=0 DOES NOT FORCE A LOCAL TIER (nine measured instances) — it only blocks the STALE-BASE path; a current-base push-time preview is still adopted. 'I set never-optimistic' is FALSE ASSURANCE a tier ran: report the OUTCOME (path adopted). Any require-the-tier control must be a DISTINCT knob with a BRANCH-CLASS PREDICATE THE GATE EVALUATES, never seat judgement (SABLE-78qck, description now written; classes earned: modifies-a-run-suite, modifies-the-selector, modifies-the-gate's-own-promote-path; mini-tier runs under max contention BY CONSTRUCTION — b0w8k hermetic fixtures or worker-covering lock is the builder's first decision).
- [2026-07-23T14:20:00-0400] PROMOTE-LIB SERIAL LANE (membership DERIVED FROM DECLARED FOOTPRINTS at dispatch time, never an enumerated list, until SABLE-e535u lands; bd swarm validate does not check intra-wave footprint overlap, SABLE-84qf9): current lane state — be4lo.1 + 21rug.1 LANDED 07-23 evening (21rug.1 gate-class at d75552e, activation complete); lane next: be4lo.6 (OPTIMUS's dispatch, fires on chuck's ACTIVE broadcast) -> be4lo.2/.7/.8, 21rug.4/.5, jd5fj.20 as they mature; NEVER parallel within the lane; Serialize-with escape FORBIDDEN for this lane even though the refusal message advertises it. Gate-class members (be4lo.7/.8, 21rug.4/.5) are serial never-optimistic re-pin hand-run. Cross-epic edge: 21rug.4 only after be4lo.3.
- [2026-07-23T15:15:00-0400] HOT-SWAP REGIME for live-symlink bins during a drain (sunsets when the pk15w registry + 26ipr + 9u86l land): (1) SERIAL promotes, second rebases on first. (1b) MERGE AND ACTIVATION MUST NOT BE SEPARATED BY OTHER PROMOTES — every promote is a pull, so a deferred activation freezes the ENTIRE queue (measured: fz8kd froze 52aym); order is merge -> declared activation IMMEDIATELY -> other promotes resume; no origin-only push tricks. (2) ACTIVATION IS A DECLARED EVENT: spawn-quiescence scoped to the EVENT not the actor ('no manager is spawning' != 'no spawn is executing' — worker suites exec the tool as subprocesses and load it in-process, invisible to a binary-name ps; a correctly-answered narrow question can produce a false clear from every respondent), TWO-LEG probe (binary exec + in-process suite loaders) re-asked within seconds, PROBE HYGIENE (exclude message/coordination processes — a message about a process matches a ps grep for it), atomic probe-and-pull (no window between clear and pull), installed-path content probe with both controls, ACTIVE broadcast, per-manager verification before next spawn. (3) WINDOW DOCTRINE: during a drain the clear window NEVER OPENS BY WAITING — windows are MADE via brokered SPAWN-HOLD (five-part form; delay NEW spawns only, interrupt nobody); the pk15w registry is what makes clearance achievable by REGISTRATION instead of absence. Current (1b) queue at the seat: wk-dispatch-delivery, 50z5g, kznzo — each activates immediately on its own merge. PRUAK OPEN CALL at the seat before wk-dispatch-delivery promotes: full comment render vs announce-only (chuck adjudicates against the bead + SABLE-aeyxk).
- [2026-07-23T16:40:00-0400, amended ~19:40 by operator word] OPERATOR PRIORITY DIRECTIVE: SPEED-RELATED BEADS/EPICS OVER GENERAL BACKLOG for the remainder of this drain — AMENDED: priority ordering stands, but the fleet must not idle thin (operator: keep the worker number higher). FILL RULE: when the priority set is walled, managers pull footprint-DISJOINT general singles (status + both-sides re-derived at pick time) rather than hold at near-zero workers; keep ~2 slots of headroom below cap so (1b) made-windows stay cheap — the promote-lib lane is the FLEET CRITICAL PATH; new spawns prefer be4lo/21rug children and jd5fj tail over shortlist singles; in-flight general workers run to completion; surviving exceptions: the dispatch-delivery family where it gates the speed path (g0elq — parser feeds the trains admission gate), 78qck capacity-allows; k9f12 parked for a genuine lull; z3j28-class parked for the operator's planning flip. Seat: speed-lane branches jump the promote queue; two promote-lib members green simultaneously is an upstream scheduling DEFECT — hold the second and report. jd5fj.11 (push-to-LANDED metric) is DELIVERED and verified — use it to prove the levers once trains land.
- [2026-07-23T20:45:00-0400] OPERATOR RULINGS (five, given to cockpit at evening shift): (1) RUN-REGISTRY CLUSTER ADMITTED TO THE SPEED SET — r5pfw probe fix + wk-runlock-instrument continuation (the pk15w second leg); TARZAN owns the cluster (stated out loud here to prevent double-assignment). Line 13's hot-swap regime sunset is the payoff target. (2) wvxb4 RULED: fresh-branch cherry-pick of afd4cd0 from the spine; wk-hotswap-runners retires un-force-pushed; NO dispatch into the frozen tree (line 10 stands) — rides with tarzan's cluster. (3) Contracts-file routing: cockpit commits via branch + gate path (operator-directed; no bare push — the rrn6r guard binds agent pushes and stays honored); executed as SABLE-ztks1 (gitignore negation per the jd5fj.5 precedent); STANDING CADENCE: cockpit re-commits this file at shift boundaries and after operator rulings. (4) PLANNING FLIP fires when the speed path is drained, agenda z3j28 (merge trains, measured against jd5fj.11 baseline from this drain) + tm8nl (footprint-at-creation); tz7h timing decided at the flip. (5) sw8uh + 926bx RELEASED from needs-human to the general pool as fill-eligible singles; bldh.7 stays operator-scheduled.
- [2026-07-23T18:20:00-0400] BUNDLE STATE: --bundle dispatches LEGAL (fz8kd landed+ACTIVE, verified on both legs — tarzan's live acceptance dispatch + chuck's by-name body read of the negative control; k9syl closed against its own criteria). FORM: comma-separated, ONE flag — xrcce's repeated-flag silent drop is STILL LIVE (its fix open, readback-echo half prioritized); leg-0 provenance readback (all ids present, all in_progress) and rendered-prompt delivery check remain the acceptance discipline for bundles. Selection derives from BOTH SIDES (candidates AND in-flight; screen-v2, occupants from git actual changed files). Interaction class, standing: guard fixes must be swept for what they newly arm (declaring footprints correctly is what activated the bundle self-denial).
