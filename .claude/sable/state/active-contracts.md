- [2026-07-23T19:05:00-0400] CONSOLIDATED AT MANAGER SHIFT BOUNDARY by cockpit, operator-approved (buckets: retire 8 discharged lines / compress 6 doctrine lines into the disciplines + parent lines / keep live regimes; history: this file entered git 2026-07-23 evening (SABLE-ztks1) — before that, bd shift/day records are the only history and the earlier "git-log of this file" claim here was false; day narrative in SABLE-1mk8f, prior consolidation 2026-07-21). BOTH pre-approved verify-then-retire items are now DISCHARGED (2026-07-23 evening): the stranded-floor line by cockpit verification, and the description-borne rule PARTIALLY — its FIELD half retired on chuck's five-rung + both-doors post-activation evidence, its TIMING half restated and KEPT. Both retirement records are dated lines below; neither was a wholesale retirement and neither should be summarised as one.
- [2026-07-21T13:42:32-0400] Merges go ONLY through sable-merge-gate; NO bare git merge/push on any integration branch. Workers self-push their worktree branch; post-push hook files for-chuck; Chuck merges via the gate. [2026-07-23 coverage note, measured: the landed rrn6r guard enforces this on agent-Bash pushes ONLY — gate promotions push via python subprocess and terminal pushes are covered by NOTHING until SABLE-eoytq's ruleset; the contract binds conduct, the guard covers one path.]
- [2026-07-21T13:42:52-0400] STATE MODEL: MERGED != INSTALLED != WIRED != RESOLVED != DELIVERED (five rungs, SABLE-xbwo2 owns the standing wiring check; SABLE-4xj4y installer symlink defect open), and ACTIVATION IS PER-FILE, never per-bead. ACTIVATION SHAPES BY FILE CLASS: plain-pinned copy -> PIN REFRESH; snapshot-pinned tool -> --pin-snapshot re-pin; SYMLINKED bin -> LOCAL PULL of the shared checkout (symlinked bins run STALE until the pull; merged-but-dark applies); repo-side files -> merge or own install step. Classify FIRST (sable-bin-install --classify PLUS test -L on the INSTALLED path, per-file — skill dirs are MIXED per file), then apply the matching shape. Acceptance observations count only after activation is verified (qp92x: ask what would have to be true for the observation to come out differently — if nothing, it is zero evidence).
- [2026-07-21T15:50:46-0400, PIN FACTS CORRECTED 2026-07-24T01:00 by cockpit on chuck's measurement] BINS POLICY. *** READ THIS CORRECTION BEFORE VERIFYING ANY PIN: THE LINE THAT USED TO STAND HERE WAS WRONG IN THE DANGEROUS DIRECTION AND WOULD HAVE TALKED A CAREFUL AGENT INTO BREAKING THE FLEET GATE. *** It named sable-d75552e5bba0 as the current sable-merge-gate pin. The truth, measured at the merge seat 2026-07-24: installed ~/.local/bin/sable-merge-gate resolves to sable-27bee596fa8b, whose sable_gate_promote_lib.py has md5 7319f6488d93511328e006668bf53c28 — BYTE-IDENTICAL to origin/tmux-only. The d75552e5bba0 snapshot (mtime 16:07:31) carries md5 5be3030e600047bcc20785496dab222c, which no longer matches HEAD. There was a FOURTH re-pin at 20:09:19 that this file never recorded. SO THE INSTALLED GATE IS CORRECT AND CURRENT AND THIS DOCUMENT WAS THE DEFECT. The failure mode it armed, which chuck avoided only by luck of method: verify the pin by IDENTIFIER (readlink equals the name in this line) -> MISMATCH -> conclude the pin is broken -> "repair" by re-pointing the bin at d75552e5bba0 -> THE FLEET MERGE GATE SILENTLY REVERTS TO A STALE PROMOTE LIB, and every instrument then reports it CORRECT because it matches the doc. He caught it by comparing the OBJECT (md5 of the promote lib against HEAD) instead of the IDENTIFIER. *** STANDING CONSEQUENCE: A PIN IS VERIFIED BY HASHING THE OBJECT AGAINST HEAD, NEVER BY MATCHING A SNAPSHOT NAME AGAINST THIS FILE. Any pin identifier written here is a HINT WITH A SHELF LIFE, not an authority; when the two disagree, the object wins and this line is what gets corrected. *** Current facts: sable-merge-gate is a VERSIONED-SNAPSHOT pin at sable-27bee596fa8b (md5 7319f648, = HEAD as of 2026-07-24 01:00; re-pinned FOUR times on 2026-07-23); sable-reconcile-handoffs pinned copy sable-c75137b2a6f5; sable-dolt-push pinned copy; sable-spawn-worker + sable-msg stay LIVE SYMLINKS (python sibling-lib imports — naive copies sever them; spawn-worker content currently carries the fz8kd+activation, cd051ae-era). IMPORT-CHECK BEFORE CONVERT gates any future pin. Refreshes are DECLARED events with before/after verification on the pinned set; '--repin' reverts OTHER pins to symlinks (33hw3) — re-verify all pins after any repin. Pinning-adjacent test suites MUST scope SABLE_LIB_DIR to scratch (33hw3 contamination incident).
- [2026-07-21T13:43:17-0400] RETIRE-ON-ACTIVATION (fleet-wide, binding, BOTH legs): a prose convention retires only when its replacing mechanism is (leg 1) VERIFIED FIRING from a live pane — never on merge or close — AND (leg 2) its DEGRADED path is demonstrated LOUD through BOTH doors (dependency ABSENT and PRESENT-BUT-UNAVAILABLE). [2026-07-23 amendment, chuck's sm269 measurement: WHICH DOOR IS SILENT IS A PER-MECHANISM PROPERTY — MEASURE BOTH EVERY TIME; wezu1's absence-loud geometry is a common case, not a rule (notes-clobber-guard is exactly inverted: bd-ABSENT is the silent door). A fail-open DECISION may stand; the REPORT must be loud; if any door degrades to silence the convention does NOT retire.] [2026-07-23T22:55 amendment, chuck's dispatch-delivery measurement — THE KINDS OF LOUD ARE NOT INTERCHANGEABLE AND THE CRITERION CANNOT SEE THE DIFFERENCE: LOUD-BY-DEGRADATION (warn, return empty, carry on) and LOUD-BY-CRASH (raise, abort the operation) both pass a not-silent test while being DIFFERENT CONTRACTS. sm269 established that WHICH door is silent is per-mechanism; this adds that WHICH KIND of loud is per-DOOR. Retirement still turns on not-silent, so a crash-loud door does NOT block it — but the kind MUST be stated per door in the evidence, and a door that is loud only by crash is a defect to file (7rp12 is the worked example: crash-loud contradicted the function's own documented degrade contract, making an additive-context dependency a hard precondition).] Live consequence: the --append-notes-never---notes discipline stays despite its guard being MERGED+INSTALLED+WIRED+DELIVERED-discriminating, until SABLE-4cad2 makes Door A loud or states the same-PATH assumption.
- [2026-07-23T20:20:00-0400] STRANDED-FLOOR LINE RETIRED (cockpit verification, operator pre-approved): positive sample = wk-provenance-guard end-to-end — genuine missed-handoff branch, resolved to SABLE-rrn6r via the structured branch= metadata leg (correct bead, correct filing-time status on SABLE-pmpd2), seat hold-with-reason as durable metadata the floor respected (no re-file), revise 36d5aa9 verified contained in origin/tmux-only, work bead closed terminal. Hand-enumeration at wind-down NO LONGER REQUIRED. Full evidence in SABLE-i5739 notes.
- [2026-07-21T13:43:50-0400] STANDING DISCIPLINES: (1) NOTES — use --append-notes, never --notes; guard landed but Door A silent, discipline holds until SABLE-4cad2 (see retire-on-activation line). Never pass inline bodies containing backticks/$()/apostrophes to bd remember / bd q / --append-notes — compose via python argv wrapper or --body-file where supported (four silent corruptions 07-22, a fifth 07-23 caught by its author; SABLE-qwthx signed-not-landed, SABLE-eu4g3 open). (2) POOL HYGIENE: non-dispatchable new beads get bd defer with an explicit un-defer condition; close-beats-defer for served coordination beads; bd ready means dispatchable-NOW; never defer a P1 blocker for hygiene. (3) INTERRUPT SAFETY: on a DIALOG-STALLED read check the working indicator first; --interrupt only with dialog markers present AND working indicator absent. (4) NEGATIVE EVIDENCE: any absence-asserting probe validates against a known positive first; verify a cited line's REACHABILITY before shipping a correction; never report a count you cannot explain per-pid. (5) SINGLE WIRING: exactly one wired copy per hook family (global ~/.claude). (6) IRON RULE regression suites are non-negotiable on any merge-spine branch. (7) SILENT-INSTRUMENT CONTRACT: 'could not assess' is a DISTINCT, LOUD outcome, never empty output; decision axis (fail-open/closed) ruled per-site, report axis always loud, the two never traded. (8) CLOSE-BEFORE-GREEN REQUIRES A WATCHED HOLD: stated lift condition + named owner + named re-opener, first-class metadata never prose (zvx4i; o9b8u/50z5g precedents; optimus runs a pre-close hold-sweep as standing practice). (9) STRANDED-CLAIM RELEASE IS THREE-STATE: in_progress+RUNNING pane = leave; +DONE pane = never release (finished awaiting close); +NO pane = release and log; fourth shape no-pane+work-MERGED = stranded-complete, seat presence-probe close. Reap panes only AFTER bead terminal state, with a pre-destruction evidence read (three pane-only sweep results saved 07-23; SABLE-hjkqf mechanizes). INSTRUMENT CAUTION until SABLE-h1fa7: sable-worker-status silently lane-scopes — fleet questions go to direct tmux enumeration. (10) BROKERED STAND-DOWN IS FIVE PARTS: named lanes, stated duration, named owner, ps confirmed both ends, EXPLICIT statement of suspended activity; a read that perturbs a load-sensitive measurement is a write. (11) SHA REFERENCES — pointer vs description (canonical on SABLE-9u86l): a POINTER to something re-derivable (the moving spine) -> quote nothing, resolve at use; a DESCRIPTION of something that must be preserved (work under revise) -> state it plus the auto-rebase expectation. (12) PURPOSE IS NOT COVERAGE: a guard's coverage is established only by reading its wiring/matcher, never inferred from its purpose. (13) DERIVATIONS HAVE A SHELF LIFE: re-derive verdict refs/pin coherence in the same breath as the action that consumes them; queue reports carry a BINDS-CURRENT-PAIR column. (14) bd list TRUNCATES SILENTLY AT 50: any absence conclusion requires --limit 0 (until wk-bdlist-truncation lands; SABLE-52aym). (15) sable-contained is the mandated tool for any containment call; a COULD-NOT-ASSESS from it is HOLD-and-hand-check, never noise.
- [2026-07-24T00:45:00-0400] *** COCKPIT BOOT DUTY, RE-ARM THE STALL POLL. BINDING ON EVERY INCOMING LINCOLN, DO THIS AT BOOT BEFORE ANYTHING ELSE. *** The 30-minute stall poll is a CronCreate job, and CronCreate is SESSION-ONLY — it is not written to disk and it DIES WITH THE SESSION, including on a cockpit recycle. So the poll silently ceases to exist at exactly the moment a shift change makes it most necessary, and nothing anywhere reports that it stopped. RE-ARM IT: CronCreate cron `13,43 * * * *` recurring, prompt = run `python3 .claude/sable/state/night-stall-probe.py` and act on the verdict (0 RUNNING report one line; 1 STALL nudge each idle manager via sable-msg to re-check the pool preferring CI-speed beads; 2 DEGRADED investigate the unreadable axis before concluding anything). Minutes 13/43 rather than 00/30 on purpose. WHAT IT DETECTS AND WHY IT IS NOT AN IDLE ALARM: managers are event-driven and their wake events ride a measurably lossy channel (ijd7j/795cf, plus qw0kj live on 2026-07-23 night — eight failed sends, landed only on --interrupt). A DROPPED WAKE EVENT AND NOTHING-TO-DO RENDER IDENTICALLY as an idle pane, so the probe alarms only on the CONJUNCTION every-manager-idle AND ready-pool-nonempty AND below-cap; any one alone is routine. The probe is read-only by design (no keys, no messages) — acting is the cockpit's job, because an auto-nudger firing on a bad reading is worse than the stall. Known false positive, do not chase it: a BUNDLED dispatch names its pane after the LEAD bead only, so non-lead bundle members show as in_progress-with-no-pane.
- [2026-07-24T00:45:00-0400] OPERATOR RULINGS FOR THE UNATTENDED NIGHT OF 2026-07-23/24 (given at log-off; these EXPIRE when the operator returns and resumes direction — do not treat them as permanent doctrine): (1) WORKER CAP 8, down from 12, counting RUNNING WORK not panes; keep ~2 slots headroom so made-windows stay cheap, i.e. practical ceiling 6. (2) CI-SPEED-IMPROVING ITEMS GET PRIORITY where a choice exists — a narrowing of the standing speed-set directive, not a replacement. (3) *** HOLD ALL GATE-CLASS PROMOTES AND ALL HOT-SWAP ACTIVATIONS UNTIL THE OPERATOR RETURNS. *** Scope: work NOT YET STARTED; anything already executing runs to completion (holds do not abort in-flight runs, 80vat). Ordinary promotes continue — SUBJECT TO (3b). (3b) CHUCK'S HOLE, ADOPTED: gate-class is NOT the only class that activates on merge. FORTY of the 42 installed sable bins are LIVE SYMLINKS into the shared worktree (only sable-merge-gate and sable-reconcile-handoffs are pinned snapshots), and for a live symlink THE PULL IS THE ACTIVATION. Verified by cockpit against the shared checkout's reflog: promotes have pulled this tree repeatedly (21:07, 20:09, 19:57, 18:58, 18:45, 18:27, 18:14 on 07-23), though the last three did not — so the hot-swap is INTERMITTENT, which is worse than reliable because it will not be firing during whatever test convinces someone it is safe. SCREEN EVERY PROMOTE CANDIDATE: three-dot diff its footprint against the spine and HOLD to morning if any changed path is a bin whose installed counterpart resolves into the worktree. DERIVE that set at screen time, never enumerate it (a re-pin or install moves files between classes; a stale membership list is the e535u defect). (4) The planning flip is HELD even if the speed path fully drains — stay in execution, park the flip with its evidence. (5) A snapshot-pinned tool's RE-PIN IS ITSELF THE ACTIVATION, not a signal about one, so a gate-class landing lands LANDED-AND-INERT tonight and the re-pin is recorded as OWED and HELD. Deferring it does NOT freeze the queue the way regime (1b) predicts, because that rule governs LIVE SYMLINKS; the snapshot pin is exactly what makes deferral safe.
- [2026-07-24T09:17:00-0400] RULING #3 (line above) LIFTED by operator on return — the night rulings otherwise expired at return; operator KEPT #3 through the morning fleet-refresh then lifted it here, SUPERSEDING #3's HOLD text. Gate-class promotes + hot-swap activations RESUME but via a STAGED OPERATOR-PRESENT plan; nothing hot-swaps unattended. (a) Two blocked fixes dispatch FIRST (not landable as-is): wk-verdict-source StrEnum display revise (SABLE-21rug.2), wk-autopromote-assert coverage-floor re-run (SABLE-21rug.4, gate DENIES at exit 27). (b) tarzan produces its OWED mutation-tested leg-3 accept on wk-runlock-trace-4. (c) The five hot-swap activations are DECLARED EVENTS (line 18 spawn-quiescence), OPERATOR-PRESENT, serialized (order HARDENED by chuck's merge-seat diff review): msg-delivery-seam(sable-msg) FIRST — swap it in THIS quiet operator-present window then GATE the rest on a DISCRIMINATING A/B/C self-test (the seam adds supersession + expiry + [composed=UTC]; a happy-path round-trip is would-pass-if-logic-deleted): A) run the branch's own test_sable_msg_integration.py (o8uti three-arm) against the NEWLY-INSTALLED bin AND the live fleet socket+session-persistence path; B) live POSITIVE round-trip — receiver acts on content + [composed=] present; C) live NEGATIVE CONTROL (load-bearing) — two distinct (sender,recipient) pairs in-window, confirm NEITHER superseded. Run the WHOLE sequence with SABLE_MSG_EXPIRY_SECONDS=0 (window mitigation for SABLE-vrs6x: the 300s default can silently expire a go-ahead to a busy chuck whose gate-class step runs to 6480s) and A must confirm expiry is LOUD not silent-drop (silent => P1, STOP). Then spawn-warn(sable-spawn-worker) -> decl-gate-input(sable-spawn-worker, REBASED ON THE POST-spawn-warn SPINE with containment re-verified AT EXECUTION not authoring — a stale rebase resolves the shared-file edit against the old tree and silently drops spawn-warn's change with NO textual conflict for the gate to catch) -> guard-install(sable-orchestration-install, CI green) -> runlock-trace-4(sable-clean-room-verify, tarzan's leg-3 mutation accept BANKED on skrdj). NOTE STEP 0 (commit this file) IS ITSELF AN ACTIVATION: the working-tree sync from local 14cb0ca to the origin tip HOT-SWAPS sable-doctor (catch-up of already-landed work; self-test sable-doctor after) — screened 2026-07-24, no other live-symlink bin in the pulled range. Gate re-pins (verdict-source, autopromote-assert) are a SEPARATE operator-present class run AFTER their fixes land, via sable-bin-install --pin-snapshot (VERIFIED 09:xx: not routed through sable-orchestration-install, so guard-install has no ordering dependency on them); each re-pin IS the activation, pin verified by HASHING THE OBJECT vs HEAD never by snapshot name (line 4), re-verify ALL pins after each --repin (33hw3 reverts others to symlinks), never interleaved; autopromote-assert stays DENIED at exit 27 until the diff-cover check is actually CARRIED (SABLE-a4i8h class), not a bare re-run. (d) COMMITTING THIS FILE is the FIRST operator-present activation: the pending t0t7m edits abort ff-checkout (chuck, 2026-07-24 morning) and committing from the shared checkout triggers the pre-push rebase -> working-tree sync -> landed bins pulled into live symlinks, so it lands with the operator before the hot-swap sequence. wk-overlap-warning already cleared (ordinary, merged-not-installed, spine 3f851ac).
- [2026-07-24T01:30:00-0400] *** A GUARD'S REAL COVERAGE IS AN INTERSECTION OF THREE FACTORS, AND EVERY SEAT KEEPS SCORING IT ON ONE. *** (chuck's formulation, extended by cockpit the same hour after it caught us both again.) INVOCATION: is the guard actually in the execution path — WIRED, not merely present. EXPOSURE: is the artifact of the shape the guard governs (a live symlink, a pinned copy). ACTIVATION: does THIS changeset actually touch it. ONLY THE INTERSECTION IS A REAL EVENT. Two measured failures within one hour on 2026-07-24, in OPPOSITE directions, both from substituting one factor for the whole: (a) cockpit and chuck both reasoned 40 bins are live symlinks THEREFORE 40 bins are at risk — scoring EXPOSURE alone — when the diff was 3 installed files, a 13x overestimate; the corrective is that exposure without a changeset is not an event. (b) One message earlier the same pair reasoned from the NAME of a pinned snapshot instead of the OBJECT and nearly reverted the fleet merge gate to a stale promote lib — an underestimate, in the dangerous direction. (c) Then both confirmed a push-time rebase FROM THE HOOK'S SOURCE without checking that core.hooksPath puts that hook in the path at all (SABLE-31b5l). Standing discipline 12 already says coverage is established by reading the WIRING and never inferred from the PURPOSE; note (c) is the same error one level in — reading the BODY, which feels MORE like diligence than reading the purpose and is still not invocation. RULE: a screen computes the intersection, never the class; and VERIFIED-BY-SOURCE and VERIFIED-BY-INVOCATION are distinct claims that must be labelled distinctly when reported.
- [2026-07-24T02:00:00-0400] *** ENUMERATING SPELLINGS INSTEAD OF EXPRESSING THE PROPERTY — FOUR INDEPENDENT INSTANCES IN ONE NIGHT, WHICH MAKES IT A HABIT OF MIND AND NOT FOUR BUGS. *** (tarzan's count, chuck's framing, cockpit-verified by execution.) THE SHAPE: a check lists the FORMS it knows about rather than stating the PROPERTY it cares about, so every unlisted form is invisible BY CONSTRUCTION, the gap is silent, and a narrow fix ("add skipif") leaves the next spelling to reopen it. The four: (1) pk15w's runner enumeration; (2) the canonical-branch pick, where recency RANKS but containment DECIDES — trace-3 was canonical because it was a patch-id superset, not because it was newest; (3) the coverage-floor skip regex; (4) the seat's exit-code table (exit 27 absent, SABLE-hsrte). INSTANCE (3) MEASURED BY COCKPIT, executing the LIVE _ADDED_SKIP_RE object rather than reading it: it matches 3 of 10 real pruning forms and MISSES 7 — skipif, both xfail decorator forms, pytest.xfail(, unittest.skipIf, unittest.skipUnless, unittest.expectedFailure. Word-boundary isolation, both directions: @pytest.mark.skip -> True but @pytest.mark.skipif -> False; @unittest.skip -> True but @unittest.skipIf -> False. THE SAME \b MISTAKE WAS MADE TWICE IN TWO ALTERNATIVES OF ONE PATTERN, which is the signature of enumerating rather than deriving — a property-shaped check has no second place to make the same error. The coverage floor therefore FAILS OPEN on essentially every conditional-pruning form in real use, and the forms it does catch are the ones nobody writes when pruning deliberately. RULE: a gate that enumerates must state, in the code, WHAT PROPERTY THE LIST IS STANDING IN FOR and why enumeration was chosen over deriving it; a list with no stated property is a silent gap awaiting its next member. Corollary for reviewers: when you fix one member of an enumeration, the bead is not done until you have asked what else belongs in the class.
- [2026-07-24T03:30:00-0400] A RELAYED STATUS CLAIM INHERITS THE RELIABILITY OF THE PATH IT RODE, NOT THE AUTHORITY OF A MEASUREMENT — and "the channel to X is down" is really "the SENDER->X leg is down", a per-directed-edge fact that must be measured AT THE OBJECT and never propagated. (chuck's formulation; SABLE-1ungf owns the per-leg principle.) Worked example, because it went through THREE seats and was wrong the whole way: optimus->chuck genuinely failed (16+16 sends, SABLE-u6386/wp7v7) -> cockpit GLOBALIZED it to "the channel to chuck is failing" -> tarzan correctly applied per-leg thinking BUT to the cockpit's misattributed leg-id, building a five-edge map whose one unmeasured edge (lincoln->chuck) he filled by ELIMINATION -> chuck re-asserted that edge as MEASURED after testing only the REVERSE edge (chuck->lincoln UP), certifying a claim about edge B from a measurement of edge A. Resolved only when cockpit captured chuck's pane and found a POSITIVE CONTROL — chuck acting on the CONTENT of lincoln's messages (holding dolt "per lincoln's ruling", quoting the ruling's own phrasing), which content could exist only if the message landed. TRUE TOPOLOGY, all edges measured: optimus->chuck DOWN is the sole failing leg; lincoln<->chuck and tarzan<->chuck UP both directions. RULES: (a) a delivery RECEIPT is not delivery — confirm by a positive control in the RECEIVER's state (pane readback, an action on the content), because the false-success class reports "delivered" on a dropped send; (b) --interrupt is NOT a reliable escalation — measured failing against a verifiably IDLE pane, so its verification leg can false-negative and manufacture duplicates; (c) when a leg is down, relay through a sender with a MEASURED-healthy leg to that recipient, never a universal relay — a relay pointed down the already-failing direction doubles it.
- [2026-07-22T23:55:00-0400] INSTALL-SHAPE CLAIMS ARE CLAIMS, NOT BACKGROUND: before any activation claim, run test -L on the INSTALLED path of every file in the landing footprint and state the shape PER FILE, emitting the DISTINCTION. A worked example inside doctrine is the most dangerous inherited claim — it arrives pre-authorised (the worker-dispatch.md symlink survived four agents in one night). Banking a lesson does not install it.
- [2026-07-23T12:40:00-0400] (a) WORKER CAP 12 (operator; SABLE_MAX_WORKERS=12 per-invocation; count RUNNING WORK not panes). (b) [RETIRED IN PART 2026-07-23T22:55 — READ THE SPLIT, DO NOT READ THIS AS A CLEAN RETIREMENT.] The FIELD half of the description-borne rule (worker-facing content must go in the DESCRIPTION because notes never render) is RETIRED: wk-dispatch-delivery landed at spine e4e7809 and ACTIVATED, so notes AND comments now render into the dispatch prompt. Evidence, chuck's five rungs with optimus supplying the leg chuck's own probe could not (presence != invocation, the xbwo2 class): DEFINED :407/:1575/:1589, INVOKED :1660/:1661 bundled + :1702 main template, POPULATED :2108-2109. Installed sable-spawn-worker is a LIVE SYMLINK so the pull IS the activation; cockpit re-verified independently at the INSTALLED path (render_comments_section x3, COMMENTS_RENDER_LIMIT x2, fetch_bead_comments x3, bogus-marker negative control x0). BOTH DOORS LOUD: Door B (bd present, call fails) degrades with a stderr warning naming the bead; Door A (bd absent) RAISES FileNotFoundError and aborts the spawn — loud, but loud BY CRASH (see the kinds-of-loud amendment on the retire-on-activation line) and contradicting fetch_bead_comments's own docstring, which promises comments are additive context and never a spawn precondition. That contradiction is SABLE-7rp12 (open, P-filed with fix + plant-and-fail spec): a real defect that does NOT block this retirement, distinguished deliberately rather than allowed to ride out on a passing criterion.
  *** (b-TIMING) SURVIVES AND IS RESTATED, BECAUSE IT WAS NEVER A FIELD RULE: THE DISPATCH PROMPT IS A SNAPSHOT TAKEN AT SPAWN. *** Verified at bin/sable-spawn-worker:2096-2109 — assemble_dispatch_prompt renders ONCE and write_text's it to a file, with fetch_bead_comments called at that instant. ANYTHING ADDED TO A BEAD AFTER ITS WORKER SPAWNS NEVER REACHES THAT WORKER IN ANY FIELD — description, notes, or comments alike — because the rendered file does not update. Get worker-facing content onto the bead BEFORE the spawn; after it, the only channel is sable-msg. Chuck narrowed this himself before the cockpit acted on his own clean recommendation: had the line retired wholesale, the fleet would have lost a still-true rule and rediscovered it the expensive way.
  IN-FLIGHT CAVEAT, expires as these close: workers spawned BEFORE the activation run the OLD binary and see DESCRIPTION ONLY (at amendment: optimus's %4 be4lo.2, %6 b0w8k, %7 be4lo.4, %8 h1fa7). Never assume an in-flight worker can read a note or comment; correct those by sable-msg. SABLE-1eeg7 section 4b already corrected by optimus — it told the incoming seat never to put worker-facing content in a note, an instruction that would have been inherited as binding after becoming false (identifier-decay shape).
  Companion landed same branch: kv44f exit-13 guard (empty description + non-trivial notes refuses loudly).
- [2026-07-23T13:45:00-0400] A BRIEF CANNOT BIND A PRE-SPAWN ACTOR (binding until SABLE-26ipr + SABLE-9u86l land): refresh_worktree rebases a REUSED worktree onto origin/tmux-only BEFORE the worker reads its prompt, silent on success. (a) NO dispatch of any kind into a frozen/held tree — the spawn itself rewrites the protected history; (b) revise-brief tip SHAs are stale by construction when the spine moves — state SHA plus auto-rebase expectation (see discipline 11); (c) description-borne rules govern what the worker READS, not what the tool does first. Fix owners: 26ipr (loud fail-closed refusal, both spawn and push legs), 9u86l (announce old->new SHA into the prompt).
- [2026-07-23T14:00:00-0400] SABLE_MG_OPTIMISTIC=0 DOES NOT FORCE A LOCAL TIER (nine measured instances) — it only blocks the STALE-BASE path; a current-base push-time preview is still adopted. 'I set never-optimistic' is FALSE ASSURANCE a tier ran: report the OUTCOME (path adopted). Any require-the-tier control must be a DISTINCT knob with a BRANCH-CLASS PREDICATE THE GATE EVALUATES, never seat judgement (SABLE-78qck, description now written; classes earned: modifies-a-run-suite, modifies-the-selector, modifies-the-gate's-own-promote-path; mini-tier runs under max contention BY CONSTRUCTION — b0w8k hermetic fixtures or worker-covering lock is the builder's first decision).
- [2026-07-23T14:20:00-0400] PROMOTE-LIB SERIAL LANE (membership DERIVED FROM DECLARED FOOTPRINTS at dispatch time, never an enumerated list, until SABLE-e535u lands; bd swarm validate does not check intra-wave footprint overlap, SABLE-84qf9): current lane state — be4lo.1 + 21rug.1 LANDED 07-23 evening (21rug.1 gate-class at d75552e, activation complete); lane next: be4lo.6 (OPTIMUS's dispatch, fires on chuck's ACTIVE broadcast) -> be4lo.2/.7/.8, 21rug.4/.5, jd5fj.20 as they mature; NEVER parallel within the lane; Serialize-with escape FORBIDDEN for this lane even though the refusal message advertises it. Gate-class members (be4lo.7/.8, 21rug.4/.5) are serial never-optimistic re-pin hand-run. Cross-epic edge: 21rug.4 only after be4lo.3.
- [2026-07-23T15:15:00-0400] HOT-SWAP REGIME for live-symlink bins during a drain (sunsets when the pk15w registry + 26ipr + 9u86l land): (1) SERIAL promotes, second rebases on first. (1b) MERGE AND ACTIVATION MUST NOT BE SEPARATED BY OTHER PROMOTES — every promote is a pull, so a deferred activation freezes the ENTIRE queue (measured: fz8kd froze 52aym); order is merge -> declared activation IMMEDIATELY -> other promotes resume; no origin-only push tricks. (2) ACTIVATION IS A DECLARED EVENT: spawn-quiescence scoped to the EVENT not the actor ('no manager is spawning' != 'no spawn is executing' — worker suites exec the tool as subprocesses and load it in-process, invisible to a binary-name ps; a correctly-answered narrow question can produce a false clear from every respondent), TWO-LEG probe (binary exec + in-process suite loaders) re-asked within seconds, PROBE HYGIENE (exclude message/coordination processes — a message about a process matches a ps grep for it), atomic probe-and-pull (no window between clear and pull), installed-path content probe with both controls, ACTIVE broadcast, per-manager verification before next spawn. (3) WINDOW DOCTRINE: during a drain the clear window NEVER OPENS BY WAITING — windows are MADE via brokered SPAWN-HOLD (five-part form; delay NEW spawns only, interrupt nobody); the pk15w registry is what makes clearance achievable by REGISTRATION instead of absence. Current (1b) queue at the seat: wk-dispatch-delivery, 50z5g, kznzo — each activates immediately on its own merge. PRUAK OPEN CALL at the seat before wk-dispatch-delivery promotes: full comment render vs announce-only (chuck adjudicates against the bead + SABLE-aeyxk).
- [2026-07-23T16:40:00-0400, amended ~19:40 by operator word] OPERATOR PRIORITY DIRECTIVE: SPEED-RELATED BEADS/EPICS OVER GENERAL BACKLOG for the remainder of this drain — AMENDED: priority ordering stands, but the fleet must not idle thin (operator: keep the worker number higher). FILL RULE: when the priority set is walled, managers pull footprint-DISJOINT general singles (status + both-sides re-derived at pick time) rather than hold at near-zero workers; keep ~2 slots of headroom below cap so (1b) made-windows stay cheap — the promote-lib lane is the FLEET CRITICAL PATH; new spawns prefer be4lo/21rug children and jd5fj tail over shortlist singles; in-flight general workers run to completion; surviving exceptions: the dispatch-delivery family where it gates the speed path (g0elq — parser feeds the trains admission gate), 78qck capacity-allows; k9f12 parked for a genuine lull; z3j28-class parked for the operator's planning flip. Seat: speed-lane branches jump the promote queue; two promote-lib members green simultaneously is an upstream scheduling DEFECT — hold the second and report. jd5fj.11 (push-to-LANDED metric) is DELIVERED and verified — use it to prove the levers once trains land.
- [2026-07-23T20:45:00-0400] OPERATOR RULINGS (five, given to cockpit at evening shift): (1) RUN-REGISTRY CLUSTER ADMITTED TO THE SPEED SET — r5pfw probe fix + wk-runlock-instrument continuation (the pk15w second leg); TARZAN owns the cluster (stated out loud here to prevent double-assignment). Line 13's hot-swap regime sunset is the payoff target. (2) wvxb4 RULED: land the work fresh-branch, frozen branches untouched — CORRECTED 2026-07-23 ~21:15 by tarzan's pre-dispatch measurement (cockpit's cherry-pick-afd4cd0 brief was WRONG: the runlock family is absent at spine and afd4cd0 modifies a file ee320ea introduced, so a lone pick has nothing to apply against; cockpit corroborated, two-commit range confirmed at 13ce2a1). Corrected procedure: rebase the ee320ea+afd4cd0 range onto origin/tmux-only RESOLVED AT DISPATCH, push as a FRESH branch name (never back to wk-runlock-instrument, which is seat-frozen do-not-merge), close-out must state the RED disposition. Ruling substance unchanged: no force-push, no pre-push bypass, frozen trees untouched — rides with tarzan's cluster. (3) Contracts-file routing: cockpit commits via branch + gate path (operator-directed; no bare push — the rrn6r guard binds agent pushes and stays honored); executed as SABLE-ztks1 (gitignore negation per the jd5fj.5 precedent); STANDING CADENCE: cockpit re-commits this file at shift boundaries and after operator rulings. (4) PLANNING FLIP fires when the speed path is drained, agenda z3j28 (merge trains, measured against jd5fj.11 baseline from this drain) + tm8nl (footprint-at-creation); tz7h timing decided at the flip. (5) sw8uh + 926bx RELEASED from needs-human to the general pool as fill-eligible singles; bldh.7 stays operator-scheduled.
- [2026-07-23T18:20:00-0400] BUNDLE STATE: --bundle dispatches LEGAL (fz8kd landed+ACTIVE, verified on both legs — tarzan's live acceptance dispatch + chuck's by-name body read of the negative control; k9syl closed against its own criteria). FORM: comma-separated, ONE flag — xrcce's repeated-flag silent drop is STILL LIVE (its fix open, readback-echo half prioritized); leg-0 provenance readback (all ids present, all in_progress) and rendered-prompt delivery check remain the acceptance discipline for bundles. Selection derives from BOTH SIDES (candidates AND in-flight; screen-v2, occupants from git actual changed files). Interaction class, standing: guard fixes must be swept for what they newly arm (declaring footprints correctly is what activated the bundle self-denial).
- [2026-07-23T22:40:00-0400] PROMOTE PRECONDITION, FLEET-WIDE AND BINDING (chuck's generalisation of a cockpit ruling, adopted after he demonstrated the defect on himself): *** BEFORE PROMOTING ANYTHING, CHECK WHETHER A HIGHER-PRIORITY MEMBER HAS A PREVIEW IN FLIGHT OR IS AWAITING PROMOTE — AND RE-DERIVE THAT AT THE PROMOTE, NEVER FROM AN EARLIER READING. *** It is a PER-MOMENT question, not a per-set one: the speed set does not need to be EMPTY, it needs to be QUIET AT THE INSTANT OF THE PROMOTE. WHY IT BELONGS TO THE ACT AND NOT TO A BEAD'S HOLD TEXT: any landing moves the spine and invalidates EVERY in-flight verification, so the constraint is a property of PROMOTING, not of the bead being promoted. The cockpit originally scoped this to one bead's hold_until (SABLE-ztks1); chuck correctly refused that scope — a per-bead hold cannot catch it, because the bead being landed is not the bead the hold is on. LIVE INSTANCE THAT PROVED IT: kznzo's hold named exactly one condition, that condition was fully satisfied, the promote was correct BY ITS OWN HOLD, and it still cost a speed-set member (be4lo.3) a full CI cycle because that member's preview was in flight. *** EVERY INDIVIDUAL GATE PASSED AND THE COMPOSITION WAS STILL WRONG. *** Mitigation when it happens anyway: re-kick immediately so the victim's clock restarts at once rather than after the report is composed — that bounds the cost to one cycle. HOLD-AUTHORING COROLLARY: a hold condition must be a TEST ANOTHER SEAT CAN EXECUTE WITHOUT RECONSTRUCTING THE AUTHOR'S INTENT (discipline 8 already demands first-class metadata; this adds that it must be OPERATIONAL, not adjectival — "behind the speed set" required knowing what the author meant and gave two opposite readings, one of which said land and one said hold).
- [2026-07-29T12:31:20-0400] INTEGRATION BRANCH IS codex-compatible FOR THIS RUN, NOT tmux-only. Retargeted 2026-07-29 via git config sable.integrationBranch, and the test base via git config sable.testCommand (--base origin/codex-compatible). BOTH are invisible in any diff and the checked-in .sable file still says tmux-only, which is stale and must NOT be trusted or reverted mid-run. Tracked on SABLE-tfzc5.
- [2026-07-29T12:31:27-0400] CHUCK, READ BEFORE CLASSIFYING ANY GATE EXIT: your standing exit-code table is INCOMPLETE while SABLE-hsrte is open. Exit 27 EXIT_COVERAGE_FLOOR and EXIT_QUARANTINED are BOTH absent from it. If the gate returns a code not in your table, ESCALATE to lincoln with the verbatim message. Do NOT improvise a disposition, and do NOT infer one from a neighbouring row.
- [2026-07-29T12:31:38-0400] FIRST ALL-CODEX FLEET RUN (dogfood, 2026-07-29): every manager and worker is Codex gpt-5.6-sol. Codex-specific defects found and FIXED in the cockpit this shift (uncommitted at time of writing, live via symlinked bin/): SABLE-6c391 pane_ready ghost-placeholder, SABLE-82k8m sandbox .git grant plus dolt-lock relocation to /tmp, SABLE-yuwrs codex queued-footer. STILL OPEN and relevant to you: SABLE-1zau6 (sable-mode handoff show exits 1 in execution mode -- read scope from 'sable-mode show' instead), SABLE-cftx3 (a Codex SessionStart hook fails, bd prime context may be missing), SABLE-gzxlk (stale CLAUDE_AGENT_NAME=lincoln in every codex pane; trust SABLE_AGENT_NAME only). If a gate, guard or hook behaves unexpectedly, treat provider difference as a live hypothesis and report to lincoln rather than working around it.
- [2026-07-30T18:25:12-0400] PRE-PUSH TEST GATE IS VACUOUS ON PUSH-BY-REF (measured 2026-07-30). The hook intercepting 'git push' runs sable.testCommand in the CURRENT WORKING DIRECTORY and does not know which ref you are pushing. Pushing a branch by explicit ref while your checkout sits on a different branch - what a merge seat does to avoid disturbing a shared checkout - tests the checked-out tree, not your commits: it reported a full 95-suite green whose only changed path was an unrelated untracked file. Workers are safe because their worktree HEAD IS their branch. Chuck: never treat a pre-push green as branch evidence; the ci-verify preview is the authority.
- [2026-07-30T18:25:24-0400] SABLE-MSG DELIVERY REALITY (measured 2026-07-30, SABLE-u2nxz). A send reporting 'undelivered after 8 attempt(s)' does NOT mean the text was lost - it is usually PARKED in the recipient's composer, unsubmitted. Failures correlate with the RECIPIENT BEING MID-TOOL-CALL, not with payload size (a 280-char send failed while a 600-char one succeeded). Consequences: put safety-critical detail on the BEAD and send a SHORT pointer message, because a bead always arrives; and long payloads render as a collapsed '[Pasted Content N chars]' block that HIDES the SABLE-MSG from= prefix, so the discriminator for injected agent traffic is unavailable exactly when the message is biggest.
