Finalize the SABLE build after the SABLE gates have passed.

Write the final-report artifact (schema `gc.build.final-report.v1`) under the
artifact root, summarizing: the methodology and interaction/review modes, the
stage artifact paths (requirements, plan, decomposition, implementation summary,
review report), confirmation that the SABLE test-evidence and scope-creep gates
passed, residual risks, and the next human action.

Record the resolved report path on the workflow root
(`gc.build.final_report_path`) before closing.
